apiVersion: v1
kind: Service
metadata:
  name: social-media-client-service
spec:
  selector:
    app: social-media-client
  ports:
    - protocol: TCP
      port: 80   # External port for frontend service
      targetPort: 3000  # Container port
  type: ClusterIP  # ClusterIP instead of LoadBalancer
