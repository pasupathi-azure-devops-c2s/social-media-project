apiVersion: v1
kind: Service
metadata:
  name: social-media-server-service
spec:
  selector:
    app: social-media-server
  ports:
    - protocol: TCP
      port: 80   # External port for backend service
      targetPort: 5000  # Container port
  type: ClusterIP  # ClusterIP instead of LoadBalancer
